# Generated by fishbowl. Edits are overwritten when the image is rebuilt.
#
# {{ researcher_user }} gets the whole machine: the boundary is around the VM, not
# inside it, and the disk is thrown away with the session — root here is for kernel
# debugging, package installs, and whatever else the analysis calls for. The packet
# filter is out of reach all the same: CAP_NET_ADMIN was dropped from the bounding set
# before sshd started, so no process it serves can alter egress, root included.
#
# {{ detonate_user }} is given no rule at all, so a sample that reads this file finds no
# way back out of the uid it was started under — not even the one that started it.
#
# sudo resets the environment it passes on, which is load-bearing here rather than
# incidental: an agent's borrowed token lives in the environment of the process that
# starts the sample, and this is what keeps it from travelling into the sample.
{{ researcher_user }} ALL=(ALL:ALL) NOPASSWD: ALL
