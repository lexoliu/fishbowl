name: Release

on:
  push:
    branches: [dev]

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  release-plz:
    name: release-plz
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
      - uses: release-plz/action@v0.5
        with:
          command: release-pr
        # A PAT rather than GITHUB_TOKEN: anything the token creates — release PRs,
        # tags, releases — fires no further workflows, and a release that fires none
        # never reaches the image job that tags the version an installed binary pulls.
        env:
          GITHUB_TOKEN: ${{ secrets.GHCR_TOKEN }}
      # Mint a short-lived crates.io token from the GitHub OIDC identity
      # instead of a stored CARGO_REGISTRY_TOKEN. Trusted publishing is
      # configured per crate on crates.io; a crate that has never been
      # published cannot be configured, so the step stays non-blocking
      # until the first `cargo publish` bootstrap lands it.
      - uses: rust-lang/crates-io-auth-action@v1
        id: crates-io-auth
        continue-on-error: true
      - uses: release-plz/action@v0.5
        with:
          command: release
        env:
          GITHUB_TOKEN: ${{ secrets.GHCR_TOKEN }}
          CARGO_REGISTRY_TOKEN: ${{ steps.crates-io-auth.outputs.token }}

