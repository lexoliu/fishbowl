name: CI

on:
  push:
    branches: [main, dev]
  pull_request:

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -D warnings

jobs:
  host:
    name: host (macOS arm64)
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
      - name: Formatting
        run: cargo fmt --all --check
      # The default members are the crates that build on a host; the ones that only
      # exist inside a session are checked by the job below, on the system they run on.
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
      - uses: taiki-e/install-action@nextest
      - name: Tests
        run: cargo nextest run
      - name: Doctests
        run: cargo test --doc

  guest:
    name: guest (Linux)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
      - name: Clippy
        run: cargo clippy -p fishbowl-gateway -p fishbowl-courier --all-targets -- -D warnings
      - uses: taiki-e/install-action@nextest
      - name: Tests
        run: cargo nextest run -p fishbowl-gateway -p fishbowl-courier
